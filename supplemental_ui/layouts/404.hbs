<!DOCTYPE html>
<html lang="{{#if site.keys.language}}{{site.keys.language}}{{else}}en{{/if}}" dir="{{#if (eq site.keys.language 'ar')}}rtl{{else}}ltr{{/if}}">
  <head>
{{> head defaultPageTitle='Page Not Found'}}
  </head>
  <body class="status-404">
{{> header}}
{{> body}}
{{> footer}}
  </body>
</html>
